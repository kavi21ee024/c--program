#include <stdio.h>

int main() {
    int arr[5]={2,4,6,8,10};
    int max;
    for(int i=1;i<5;i++)
    {
        if(max<arr[i]){
        max=arr[i];
    }
    }
    printf("%d",max);
    return 0;
}
